neo:
  probe:
    http:
      user-resolver: session # valid values are session, jwt, header
      user-session-key: x-user-id # the key for the entry in the session applicable for user-resolver = session
